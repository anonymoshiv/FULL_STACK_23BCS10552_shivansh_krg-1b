# Development datasource: default to H2 in-memory so the app runs without PostgreSQL.
# To use PostgreSQL, comment the H2 block and uncomment the PostgreSQL block below.

# H2 (development)
# spring.datasource.url=jdbc:h2:mem:grievancedb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
# spring.datasource.driver-class-name=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.defer-datasource-initialization=true
# spring.sql.init.mode=always

# PostgreSQL example (uncomment and set values to use Postgres)
spring.datasource.url=jdbc:postgresql://localhost:5432/grievance_db
spring.datasource.username=postgres
spring.datasource.password=Shiv#2105
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

server.port=8080
